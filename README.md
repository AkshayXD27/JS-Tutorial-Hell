This is a set of my Javascript programs that, I have executed on my own [reference = SuperSimpleDev]
1) rock-paper-scissors.html [ Basic html structure contains 4 buttons rock paper scissors and reset respectively ]
   Learnt about LocalStorage and Stored Users scores in local database so when refreshed the scores remain same
   Learnt about JSON (javascript object notation) and how it is similar to javascript objects and used JSON.Parse and JSON.Stringify to store data as objects and later convert them in json format
2) Main.html [Connecting rock-paper-scissors.html and creating a subscribe button]
   Learnt about DOM Document object module and how every html element is a javascript object (dom)
   Created a button called subscribe and changed it to subcribed upon user clicks by calling a function
   Used localStorage to store the current / previous status of the button if it was subscribed before refreshing it changes back into subcribed / returns the button to the previous state
   The function checks the current button status and changes it to Subscribe & vice versa
      
